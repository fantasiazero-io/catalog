package main

import (
	"testing"
)

func Create_new_character(t *testing.T) {

	// id, _ := uuid.NewUUID()
	// got := aggregates.NewCharacter(id)

	// if id != got.Id {
	// 	t.Errorf("got %q, wanted %q", got, id)
	// }
}
